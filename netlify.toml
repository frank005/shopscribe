[build]
  publish = "build"
  command = "npm install --prefer-offline --no-audit --no-fund && npm run build"
  base = "."

[build.environment]
  NODE_VERSION = "18"
  CI = "false"
  NODE_ENV = "production"
  NPM_CONFIG_PRODUCTION = "false"
  NPM_CONFIG_AUDIT = "false"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
  # pako kept external — esbuild can't trace it through AccessToken2.js's try/catch require,
  # so leave it as a runtime require and let Netlify ship it in the function's node_modules.
  external_node_modules = ["pako"]
  included_files = ["netlify/functions/utils/**"]

[dev]
  command = "npm start"
  port = 8888
  targetPort = 3000
